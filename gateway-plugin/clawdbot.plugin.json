{
  "id": "pwa",
  "name": "PWA",
  "version": "0.1.0",
  "description": "PWA (Progressive Web App) channel plugin for OpenClaw Gateway",
  "author": "",
  "license": "MIT",
  "configSchema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "title": "Enabled",
        "description": "Enable the PWA channel plugin",
        "default": true
      }
    }
  },
  "uiHints": {
    "enabled": {
      "label": "Enable PWA Channel",
      "description": "Allow connections from PWA clients"
    }
  }
}
