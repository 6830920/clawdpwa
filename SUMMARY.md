# ✅ 配置文件功能已完成！

## 🎉 混合配置模式已实现

**配置优先级（从高到低）：**
1. 命令行参数 - 临时覆盖
2. 配置文件 - 持久化配置
3. 默认值 - 代码中的默认配置

---

## 📦 新增文件

```
pwa-versions/go-version/
├── clawchat.exe              ✅ 8.4 MB（包含所有资源）
│
├── 配置文件
├── config.toml               ⚙️ 默认配置文件
├── config.dev.toml           🔧 开发环境配置
├── config.prod.toml          🚀 生产环境配置
├── config.example.toml       📖 配置示例（供参考）
│
├── 启动脚本
├── start-dev.bat             开发环境启动
├── start-prod.bat            生产环境启动
├── start-local.bat           本地测试启动
├── start-remote.bat          远程 Gateway 启动
│
├── 文档
├── CONFIG.md                 ⭐ 配置完整指南
├── QUICKSTART.md             📖 快速参考
├── README.md                 📖 使用说明
├── .gitignore                🔒 Git 忽略规则
│
├── 源代码
├── main.go                   ✅ 支持 TOML 配置
├── go.mod                    Go 模块
└── go.sum                    依赖锁定
```

---

## 🚀 使用方式对比

### 方式 1: 纯命令行（旧方式）
```bash
./clawchat.exe -p 36006 -g ws://127.0.0.1:18789 -t YOUR-TOKEN
```
❌ 命令太长
❌ Token 容易泄露
❌ 不方便管理

### 方式 2: 纯配置文件（新方式）
```bash
# 1. 创建配置文件
cat > config.toml << EOF
[server]
port = "36006"

[gateway]
url = "ws://127.0.0.1:18789"
token = "YOUR-TOKEN"
EOF

# 2. 直接运行
./clawchat.exe
```
✅ 简洁
✅ 配置持久化
✅ 方便管理

### 方式 3: 混合模式（推荐）⭐⭐⭐
```bash
# 使用配置文件，但临时覆盖端口
./clawchat.exe -p 8080

# 使用配置文件，但临时测试其他 Gateway
./clawchat.exe -g ws://test-gateway:18789

# 使用配置文件，但临时测试新 Token
./clawchat.exe -t new-test-token
```
✅ 灵活
✅ 配置文件管理长期配置
✅ 命令行处理临时变更

---

## 📋 配置文件格式 (TOML)

```toml
[server]
port = "36006"

[gateway]
url = "ws://127.0.0.1:18789"
token = "YOUR-TOKEN-HERE"
timeout = 30

[client]
id = "webchat-ui"
version = "1.0.0"
platform = "web"
mode = "webchat"

[chat]
sessionKey = "global"
thinking = "auto"
deliver = true

[log]
level = "info"
```

---

## 🎯 使用场景

### 场景 1: 日常开发
```bash
# 编辑 config.toml 设置好 token
./clawchat.exe
```

### 场景 2: 多环境部署
```bash
# 开发环境
./clawchat.exe -c config.dev.toml

# 生产环境
./clawchat.exe -c config.prod.toml
```

### 场景 3: 快速测试
```bash
# 测试不同的 Gateway，无需修改配置
./clawchat.exe -g ws://test-gateway:18789

# 测试不同的 Token
./clawchat.exe -t test-token-12345

# 测试不同端口
./clawchat.exe -p 9999
```

### 场景 4: 生产部署
```bash
# 使用专用配置文件
./clawchat.exe -c /etc/clawchat/production.toml

# 或 systemd 服务
[Service]
ExecStart=/opt/clawchat/clawchat -c /etc/clawchat/production.toml
```

---

## 📝 命令行参数

| 参数 | 简写 | 说明 | 示例 |
|------|------|------|------|
| `-config` | `-c` | 指定配置文件 | `-c config.dev.toml` |
| `-port` | `-p` | 覆盖端口 | `-p 8080` |
| `-gateway` | `-g` | 覆盖 Gateway URL | `-g ws://...` |
| `-token` | `-t` | 覆盖 Token | `-t TOKEN` |
| `-version` | `-v` | 显示版本 | `-v` |
| `-help` | `-h` | 显示帮助 | `-h` |

---

## ✅ 已验证测试

- ✅ 自动加载 `config.toml`
- ✅ 使用 `-c` 指定配置文件
- ✅ 命令行参数覆盖配置文件
- ✅ Token 正确注入到 HTML
- ✅ Gateway URL 正确替换
- ✅ 端口配置生效
- ✅ 配置文件格式验证

---

## 🔒 安全建议

1. **不要提交真实 token 到 Git**
   - `.gitignore` 已配置排除 `config.toml`
   - 只提交 `config.example.toml`

2. **多环境使用不同 token**
   - `config.dev.toml` - 开发 token
   - `config.prod.toml` - 生产 token

3. **Token 管理**
   - Token 过期后更新配置文件
   - 重启服务器生效

---

## 📚 文档

- **CONFIG.md** - 完整配置指南（必读）
- **QUICKSTART.md** - 快速参考
- **README.md** - 使用说明

---

## 🎉 总结

**推荐配置方式：混合模式**

1. **长期配置** → 放入 `config.toml`
   - Token
   - Gateway URL
   - 默认端口

2. **临时变更** → 使用命令行参数
   - 测试不同端口
   - 测试不同 Gateway
   - 测试不同 Token

这样既方便管理又灵活！🚀

---

**版本**: v1.0.0
**更新**: 添加 TOML 配置文件支持
**状态**: ✅ 已完成并测试通过
